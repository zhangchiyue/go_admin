package server

import (
	"github.com/gin-gonic/gin"
)

func setMiddlewares(e *gin.Engine) {
	//e.Use(middleware.RecoveryMiddleware())
	//e.Use(middleware.ReqRateMiddleware())
	//e.Use(middleware.ReqIDMiddleware())
	//e.Use(middleware.RequestLogger())
	//e.Use(middleware.ResponseLogger())
}
